{
  "name": "vitalorgans",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install",
    "server": "node ./server/server.js",
    "client": "cd client && npm run dev"
  },
  "description": "",
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
